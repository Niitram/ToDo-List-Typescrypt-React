/* // JavaScript
function sum(a, b) {
  return a + b;
}

//Typescript
function sum(a:number, b:number): number  {
  return a + b;
} */

/* // JavaScript
function isEven(number) {
  return number % 2 === 0;
} */
// Typescript



interface Props {
  // Propiedades aquí
};

const asdqwd: React.FC<Props> = (qadqwd) => {
  return (
    <div>
      wfwef
    </div>
  );
};

export default asdqwd;